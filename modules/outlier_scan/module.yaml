name: outlier_scan
title: Outlier Scan
version: 0.0.1
description: Detect numeric outliers using IQR.
public: true
category: Data
entrypoints:
  api: modules.outlier_scan.tool.app:app
mount: /data/outliers
flows:
  after_success:
    - target: category_guess
    - target: csvclean
    - target: csvcolumns